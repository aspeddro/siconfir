# Contributing

First of all, thank you very much for contributing to `sincofir`.

## General

- Format code with `styler::style_dir()`
- Lint with `lintr::lint_dir()`
- Add tests to new functions, make sure tests are passing `devtools::test()`
- Add documentation and update with `devtoools::document()`